<% provide :title, t("user.edit.title") %>
<% provide :button_text, t("user.edit.change") %>
<h1><%= t "user.edit.update" %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t("user.edit.change"), Settings.links.gravartar_change,
      target: "_blank" %>
    </div>
  </div>
</div>
